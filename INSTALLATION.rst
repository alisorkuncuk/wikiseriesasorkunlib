============
Installation
============

At the command line::

    $ pip install wikiseriesasorkunlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriesasorkunlib
    $ pip install wikiseriesasorkunlib

Or, if you are using pipenv::

    $ pipenv install wikiseriesasorkunlib

